---
import Container from "@components/Container.astro"

type Props = {
  size?: "sm" | "md" | "lg" | "xl" | "2xl"
}

const { size = "md" } = Astro.props as Props
---

<div class="pt-6 pb-5">
  <Container size={size}>
    <slot/>
  </Container>
  
</div>